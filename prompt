"Fix ATLAS source relevance — currently sources shown contain matching keywords but don't directly answer the user question. I want ONLY sources that directly answer the question to be shown.
PROBLEM 1 — Source ranking wrong:
The current scoring formula is 0.35 keyword + 0.65 vector. This is better but still surfaces tangentially related documents. The ranking needs to prioritize documents where the answer to the question actually EXISTS in the chunk text — not just documents that mention related words.
Fix: Add an answer-presence check to the scoring. For each retrieved chunk — check if the chunk text contains a direct answer pattern for the question type. For HOW questions — chunk should contain steps or instructions. For WHAT questions — chunk should contain a definition or explanation. For WHERE/WHEN questions — chunk should contain specific location or time information. Add 0.3 bonus to chunks that pass the answer-presence check. This makes chunks that actually CONTAIN the answer rank above chunks that merely MENTION related terms.
PROBLEM 2 — Too many marginally related sources shown:
Currently showing up to 5 sources even when only 1-2 are directly relevant. The threshold of 0.40 is still too low — marginal sources sneak through.
Fix: Implement a score gap filter. After reranking — calculate the score gap between the top source and each subsequent source. If a source scores more than 0.25 below the top source score — cut off there. This means if top source is 85% and next is 55% — only show the top source. The list stops where there is a significant drop in relevance. Never show more than 5 sources. Never show sources below absolute threshold of 0.45.
PROBLEM 3 — Source URL not matching the best answer:
The source link shown in the citation card must point to the exact Confluence page that contains the answer — not a parent page or related page. Verify in vector_store.py that the URL stored in Qdrant payload is the direct page URL including the page ID — not just the space URL. If URL is missing or only has space key — fix the ingestion to store the full direct page URL in the chunk payload.
PROBLEM 4 — Verify source title matches content:
Check that the page_title stored in Qdrant payload matches the actual Confluence page title that contains the answer chunk. If chunks from a page called 'MPA Gateway Guide' are being retrieved but showing title '2020 Status Wiki' — there is a metadata mismatch in the ingestion pipeline. Fix the chunking in text_splitter.py and ingest.py to ensure each chunk carries the correct parent page title and URL.
IMPLEMENTATION ORDER:
1. Fix vector_store.py — verify URL and title in payload
2. Fix reranker.py — add answer-presence bonus and score gap filter
3. Fix chat.py — raise absolute threshold to 0.45, implement score gap cutoff
4. Run all tests — target 456/456 passing
5. Test these 5 queries and show before/after source titles and URLs:
— "What is MPA gateway activation?"
— "How do I set up development environment?"
— "What are the API rate limits?"
— "What is the SQS retry policy?"
— "How do I request Confluence access?"
Show me the source TITLE and URL for each query before and after fix.
Show me all file changes before applying them."
