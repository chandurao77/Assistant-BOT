Tell Claude Code:

"Before applying, fix 3 things:
1. Use rep.severity.value for severity 
   weight, not string matching on message 
   text — a message containing 'exception' 
   might be INFO level.
2. Sort all_errors by ts_epoch BEFORE 
   computing the midpoint spike fallback — 
   currently midpoint of unsorted list is 
   a random timestamp.
3. The first_bonus is always 0.3 because 
   you iterate unique dict keys — either 
   remove it or make it only apply to the 
   genuinely earliest error in the window.
Then apply all fixes."
