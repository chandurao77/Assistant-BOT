Looking at all 3 screenshots carefully, I can see two distinct bugs in the QueryQuokka UI.

## Bug 1: Tab Navigation Wrong (Cross-Domain Investigation)

**What you're seeing:**
- In Image 2, there are tabs: `Overview` | `Gateway Provisioning` | **`FAH Activation`** | `Interleaved`
- When you click **MPA** sub-item → it navigates to **FAH** tab
- When you click **FAH** sub-item → nothing happens

**Root Cause — likely a tab index/ID mismatch:**

```javascript
// BUG: Tab IDs are probably misaligned
const SUB_INVESTIGATION_TABS = [
  { id: 'gateway',  label: 'Gateway Provisioning' },  // index 0
  { id: 'fah',      label: 'FAH Activation' },         // index 1  ← MPA click lands here
  { id: 'mpa',      label: 'MPA' },                    // index 2  ← FAH click goes nowhere
];

// FIX: Ensure click handler uses the item's ID, not its array index
const handleSubInvestigationClick = (item) => {
  // WRONG — uses position
  navigateToTab(investigations.indexOf(item));
  
  // CORRECT — uses explicit ID
  navigateToTab(item.investigationType); // 'mpa' | 'fah'
};
```

**Also check the sidebar item render:**
```jsx
// BUG: key and onClick might be swapped/offset
{subInvestigations.map((inv, idx) => (
  <SidebarItem
    key={inv.id}
    onClick={() => setActiveTab(idx)}  // ← idx is wrong if list order != tab order
    // FIX:
    onClick={() => setActiveTab(inv.type)}
  />
))}
```

---

## Bug 2: Log Volume Chart Compression (from previous message)

Already diagnosed — the **748-event** investigation collapses the x-axis. Still needs the `autoSkip` + `barThickness` fix.

---

## Quick Fix Checklist

| # | Bug | File to check | Fix |
|---|-----|---------------|-----|
| 1 | MPA→FAH navigation | `InvestigationSidebar.tsx` | Use `inv.type` not `idx` |
| 2 | FAH click does nothing | Tab routing logic | Verify FAH has a registered route/handler |
| 3 | Chart compression | `LogVolumeChart.tsx` | Add `autoSkip: true` on x-axis |

---

Want me to look at the actual sidebar and tab routing source files? If you share the component paths I can write the exact patch.
